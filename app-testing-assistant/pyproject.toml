[project]
name = "app-testing-assistant"
version = "0.1.0"
description = "MCP server that helps QA testers and developers learn to write integration tests"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.23.1",
]
